# Builds and deploys the email gateway.
#
# Pattern cloned from LeadLovers.LandingPages.Leadia:
# stage 1 builds the Docker image and pushes it to Docker Hub via the
# `CloudLovers - Docker Hub` service connection; stage 2 stages
# docker-compose.yml onto the OCI agent and runs `docker compose up -d`.

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - apps/gateway/**
      - apps/lib/**
      - package.json
      - bun.lock
      - bunfig.toml
      - tsconfig.base.json
    exclude:
      - apps/lib/README.md
      - apps/lib/PRD.md
      - apps/lib/devops/github-actions/**

pool:
  name: OCI-LeadLovers022

variables:
  # Adjust the variable-group name to whatever exists in this Azure DevOps
  # project; it must provide DATABASE_URL, REDIS_URL, and GATEWAY_API_KEY
  # (all secret).
  - group: leadlovers-ses-production
  - name: imageName
    value: cloudlovers/leadlovers-email-gateway
  - name: imageTag
    value: $(Build.BuildNumber)
  - name: containerName
    value: leadlovers-email-gateway
  - name: portInner
    value: '8787'
  - name: portOuter
    value: '8787'
  - name: networkName
    value: leadlovers-internal
  - name: deployPath
    value: /var/docker/leadlovers/email-gateway/production
  - name: projectName
    value: leadlovers-email-gateway

stages:
  - stage: BuildAndPush
    displayName: Build and push image
    jobs:
      - job: Build
        displayName: docker build + push
        steps:
          - task: Docker@2
            displayName: Login, build and push
            inputs:
              containerRegistry: 'CloudLovers - Docker Hub'
              repository: $(imageName)
              command: buildAndPush
              Dockerfile: apps/gateway/devops/Dockerfile
              buildContext: $(Build.SourcesDirectory)
              tags: |
                $(imageTag)
                latest

  - stage: Deploy
    displayName: Deploy to OCI
    dependsOn: BuildAndPush
    condition: succeeded()
    jobs:
      - deployment: Deploy
        displayName: docker compose up
        environment: Production
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self
                - script: |
                    set -euo pipefail

                    # Ensure the shared internal network exists. docker-compose.yml
                    # marks it `external: true`, so compose expects us to have
                    # created it out-of-band. Idempotent: errors if already present
                    # and we swallow that. Drop this once the network is managed
                    # by infra-as-code or another canonical owner.
                    docker network create "$(networkName)" 2>/dev/null || true

                    mkdir -p "$(deployPath)"
                    cp apps/gateway/devops/docker-compose.yml "$(deployPath)/docker-compose.yml"
                    cd "$(deployPath)"
                    cat > .env <<EOF
                    DOCKER_IMAGE=docker.io/$(imageName):$(imageTag)
                    CONTAINER_NAME=$(containerName)
                    PORT_INNER=$(portInner)
                    PORT_OUTER=$(portOuter)
                    NETWORK_NAME=$(networkName)
                    DATABASE_URL=$DATABASE_URL
                    REDIS_URL=$REDIS_URL
                    GATEWAY_API_KEY=$GATEWAY_API_KEY
                    EOF
                    docker compose -p "$(projectName)" pull
                    docker compose -p "$(projectName)" up -d --remove-orphans
                  displayName: stage compose, write .env, compose up
                  env:
                    DATABASE_URL: $(DATABASE_URL)
                    REDIS_URL: $(REDIS_URL)
                    GATEWAY_API_KEY: $(GATEWAY_API_KEY)
