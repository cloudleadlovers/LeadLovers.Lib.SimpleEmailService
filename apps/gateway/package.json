{
  "name": "@leadlovers/simple-email-gateway",
  "private": true,
  "version": "0.1.0",
  "description": "HTTP gateway in front of @leadlovers/simple-email-service for legacy consumers.",
  "type": "module",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build step; gateway runs via Bun directly'"
  },
  "dependencies": {
    "@leadlovers/simple-email-service": "workspace:*",
    "@prisma/adapter-mssql": "^7.0.0",
    "@prisma/client": "^7.0.0",
    "redis": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
