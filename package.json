{
  "name": "leadlovers-simple-email-service-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "typecheck": "bun --filter '*' run typecheck",
    "test": "bun --filter '*' run test",
    "build": "bun --filter '*' run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
